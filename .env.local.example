# Local-only overrides for HOSTED Supabase
# Copy to .env.local and fill values. Do not commit .env.local.

# Frontend (public)
VITE_USE_SUPABASE=true
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<your_anon_key>

# Maps (public)
# Optional: used by Leaflet tile layer (Geoapify). If unset, falls back to OpenStreetMap tiles.
VITE_GEOAPIFY_API_KEY=your_geoapify_api_key_here

# Direct Postgres connectivity test (private)
# Get this from Supabase Dashboard -> Project Settings -> Database -> Connection string
# Recommended format includes sslmode=require
SUPABASE_DB_URL=postgresql://postgres:<DB_PASSWORD>@db.<project-ref>.supabase.co:5432/postgres?sslmode=require

# For Node-based authenticated DB operations test (private)
# Create a test user in Supabase Auth, then set these:
TEST_USER_EMAIL=you@example.com
TEST_USER_PASSWORD=your_password
